# Optimize

For optimizing a program, what we do:

- Keep feature correctness while making changes.
- Reduce execution time.
- Reduce resource usage such as CPU, memory, and storage.
- Simplify the workflow so the program is easier to use.
- Improve maintainability so the program is easier to change and extend.
- Build or improve tools that speed up feature development.
